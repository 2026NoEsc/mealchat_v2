# Figma 좌표 스펙

Figma 파일 `xBf3b09D6Bj1dTiCixt25e` / 페이지 `Application UI` (`0:1`).

모든 화면 프레임은 **220 × 486**. 아래 좌표는 전부 프레임 절대값(Figma 단위)이며,
`src/theme/scale.ts` 의 `s()` 로 기기 폭에 비례 변환한다.
상태바(노치) 영역은 상단 **28**. `y - 28` 이 SafeArea 기준 오프셋.

> 페이지 전체 `get_metadata` 는 응답이 잘려 실패한다. 섹션 단위로만 조회 가능하다.

## 섹션 ID

| 섹션 | node ID |
|---|---|
| 1_로그인 | `314:592` |
| 1_회원가입 | `309:2256` |
| 1.5_취향게임 | `296:2851` |
| 2_홈 | `309:1510` |
| 2_일정_조율 | `309:1430` |
| 2_채팅방 | `309:1429` |
| 2_프로필 | `309:1428` |

## 공용 컴포넌트 (Design System 페이지 `40:168`)

| 컴포넌트 | node ID | 크기 |
|---|---|---|
| AppHeader | `46:22` | 220 × 42 |
| BottomNav1~4 | `83:403` `40:177` `315:4564` `315:4565` | 220 × 38 |
| RoomCard | `47:13` | 213 × 82.4 |
| AD Viewer | `83:435` | 213 × 127.594 |
| 메이트 선택 | `141:182` | 207 × 77 |
| AccentButton / AndNext | `378:2894` / `378:2906` | 197 × 28 |
| CompleteButton / AndNext | `309:2483` / `309:2484` | 197 × 28 |
| AddButton | `525:3216` | 197 × 28 |
| DangerButton | `309:2422` | 197 × 28 |
| BackButton | `525:3226` | 14 × 13 |
| Profile1~4 | `52:126` ~ `52:129` | 27~31 × 27~28 |
| SettlementCard | `780:418` (`2_홈` 섹션 안) | 197 × 78 · variant `상태=진행중/완료` |

## 구현 완료

- **로그인** `150:124` → [LoginScreen](../src/screens/LoginScreen.tsx)
- **홈/메인** `309:1064` → [HomeScreen](../src/screens/HomeScreen.tsx)
- **정산** `779:352` → [SettlementsScreen](../src/screens/settlement/SettlementsScreen.tsx)
  - Figma 에 원본 시안이 없던 화면이다. 코드를 먼저 만들고 파일에 옮겨 그렸다 (`2_홈` 섹션).
  - 카드는 `SettlementCard` 컴포넌트 세트로 두고 인스턴스를 놓았다.
- **알림 패널** `549:3408` (196 × 350) → [NotificationPanel](../src/components/NotificationPanel.tsx)
- **회원가입 5화면** → [screens/signup](../src/screens/signup)
  - 시작 `150:125` / 개인정보 `150:121` / 구글캘린더 `150:122` / 취향분석 `150:123` / 약관동의 `150:126`
  - 2~4단계는 [SignupIllustrationScreen](../src/screens/signup/SignupIllustrationScreen.tsx) 공통 레이아웃 사용
- **취향게임 6화면** `296:2866`~`296:2966` → [TasteGameScreen](../src/screens/taste/TasteGameScreen.tsx)
  - 6개 프레임이 데이터만 다르고 레이아웃이 동일해서 index state 를 쓰는 단일 화면으로 구현
  - 음식 이미지는 `assets/taste/` 에 Figma export 로 저장

- **프로필 홈** `159:544` → [ProfileHomeScreen](../src/screens/profile/ProfileHomeScreen.tsx)
- **채팅방 홈** `549:3507` → [ChatHomeScreen](../src/screens/chat/ChatHomeScreen.tsx)
- **일정 조율** `309:1077` → [ScheduleHomeScreen](../src/screens/schedule/ScheduleHomeScreen.tsx)

> 하단 탭 4개(홈·일정 조율·채팅방·프로필)가 모두 실제 화면이다. `ComingSoonScreen` 은 더 이상 사용하지 않는다.

- **채팅방 (대화)** `315:4324` → [ChatRoomScreen](../src/screens/chat/ChatRoomScreen.tsx)
  - roomHeader y30 h44 / 확정 배너 y83 h18 / chatScroll x11.5 y104 w197 gap8 / 액션바 y407 h43 / 입력바 y448 h38
  - 말풍선 radius 는 보낸 쪽만 우상단 2, 받은 쪽만 좌상단 2 (나머지 9)
- **채팅 하단 시트 3종** → [ChatRoomSheets](../src/screens/chat/ChatRoomSheets.tsx),
  공통 셸은 [BottomSheet](../src/components/BottomSheet.tsx)
  - 일정 조율 `553:408` / 메뉴 정하기 `553:698` / N빵 정산 `553:727`
  - 시트에서 확정하면 채팅에 시스템 말풍선으로 결과가 남는다
- **일정 추가 4화면** → [screens/schedule](../src/screens/schedule)
  - STEP 1 디테일 `309:1065` / STEP 2 시간 `160:733` / STEP 3 AI 추천 `159:491` / 확정 `160:827`
  - STEP 1·2 상단은 [ScheduleStepHeader](../src/screens/schedule/ScheduleStepHeader.tsx) 공용
  - STEP 1 에서 입력한 약속 이름이 확정 카드와 채팅방 헤더까지 파라미터로 전달된다
  - AI 추천 화면의 `309:1060` "가능한 시간을 드래그해서 표시해 주세요" 는 STEP 2 에서 복사된
    잔재로 보여 구현에서 제외했다

## 미구현 화면

남은 7개. 각 항목은 Figma 렌더에서 직접 확인한 구성이다.

### 채팅방 (`309:1429`)

**방 상세정보 `159:604`** — 220 × 486, 하단 탭(채팅방) 유지
- 상단은 채팅방과 같은 roomHeader, 그 아래 "방 상세정보" 타이틀
- 초대 코드 카드 — `VF4HLD` 를 오렌지 대형 텍스트로, 우측에 `복사` 링크
- 약속 장소 카드 — "조선칼국수 하단점" + "부산 사하구 하단동 · 도보 8분", 우측 `변경` 링크
- 멤버 카드 — 헤더 "멤버 4명" + 우측 `+ 초대`, 행마다 아바타·이름·역할 배지
  (모아(나) = `방장`, 두두/또리/웰링 = `메이트`)
- 하단 `방 나가기` — 기존 [DangerButton](../src/components/ui/Button.tsx) 재사용

**참여 멤버 시트 `553:768`** — 236 × 302
- [BottomSheet](../src/components/BottomSheet.tsx) 셸 재사용 가능
- 타이틀 "참여 멤버", 서브 "멤버 4명 · 초대코드 VF4HLD"
- 행 = 아바타 + 이름 + 접속 상태 + 우측 역할 배지
  - 모아(나) / 온라인 / `방장` — 이 행만 `#FFF5EB` 배경 + 오렌지 보더로 강조
  - 두두 / 온라인, 또리 / 3시간 전, 웰링 / 온라인 — 모두 `메이트`
- CTA `+ 메이트 초대`
- 채팅방 하단 액션바의 `멤버` 버튼에 연결하면 된다 (현재 핸들러 비어 있음)

**이모티콘 패널 `555:416`** — 240 × 156
- 헤더 "이모티콘" + 우측 `전체 보기 →`
- 4열 × 2행 = 8칸 스티커 그리드, 각 칸은 흰 카드
- 입력바의 Smile 아이콘에 연결하고, 선택 시 `ChatRoomScreen` 의 `sticker` 메시지로 append

### 프로필 (`309:1428`)

**프로필 수정 `309:1086`** — 228 × 494, 하단 탭(프로필) 유지
- 카드 안에 로고 일러스트 + "프로필 수정" 타이틀
- 필드 순서: 닉네임(`나야나`) / 이메일(`nayana@gmail.com`) / 비밀번호 변경 / 비밀번호 재확인
- 계좌번호 — 은행 드롭다운(`549:3366`) + 번호 입력 2분할
- 생년월일 — `2002` 년 / `12` 월 / `20` 일 3분할
- 회원가입 개인정보 입력(`150:121`)과 필드 구성이 거의 같아
  [SignupPersonalScreen](../src/screens/signup/SignupPersonalScreen.tsx) 을 참고하면 된다

**정보 공개 범위 `256:2494`** — 220 × 486
- 타이틀 "정보 공개 범위" + 서브 "밥약 메이트에게 보이는 정보를 선택하세요"
- 기본 정보 카드 — 생년월일(`1111년 11월 11일`) ON / 성별(`남자`) ON / 한마디 멘트 OFF
- 민감 정보 카드 — 계좌번호("정산 시에만 공개") OFF / 출발지 주소("중간 지점 계산에만 사용") ON
- 각 행은 라벨 + 회색 부연설명 + 우측 토글. 토글은
  [ScheduleHomeScreen](../src/screens/schedule/ScheduleHomeScreen.tsx) 의 자동 연동 토글과 동일 형태
- 카드 아래 자물쇠 안내 문구, 하단 CTA `저장하기`

**출발지 설정 (지도 위치 지정) `256:2333`** — 220 × 486
- 타이틀 "출발지 설정" + 서브 "중간 지점 계산에 사용됩니다"
- 검색창 "주소 또는 장소 검색"
- 그 아래 지도 영역 — Figma 는 회색 격자 플레이스홀더. 실제 지도 SDK 는 미정
- 하단 카드 — "선택한 위치" / `부산 사하구 낙동대로550번길 37` / "동아대학교 승학캠퍼스 근처"
- CTA `이 위치로 저장`

**은행 선택 드롭다운 `549:3366`** — 50 × 132, 화면이 아니라 공용 오버레이
- 트리거 라벨 `은행 선택 ∨`, 펼치면 항목 8개가 1px 구분선으로 나뉜다
- 국민은행 / 기업은행 / 농협 / 네이버페이 / 부산은행 / 우리은행 / 카카오페이 / 토스페이
- 항목 텍스트 7px `#999`, 트리거 텍스트 `#5F5E5B`, 카드 radius 5 + shadow `0 0 4 rgba(169,169,169,0.25)`
- 회원가입 개인정보 입력과 프로필 수정의 계좌번호 필드가 공유한다

### 일정 조율 (`309:1430`)

미구현 화면 없음 — STEP 1~3 과 확정까지 모두 구현됐다.

일정 조율 화면 색상: 이벤트 바 `#5B9BD5`(개인) `#B483C8`(발표) `#9C9C9C`(기기 동기화),
일요일 `#F5556B` / 토요일 `#3BA3F5`, 강조 셀 `#FFF5EB`, 선택 셀 테두리 `#FF9900`

## 알려진 불일치

- **요일 표기 불일치 → 해결.** 요일은 [src/lib/calendar.ts](../src/lib/calendar.ts) 에서만 계산하며
  기준은 **실제 달력**(`2026-08-01 = 토`)이다. 화면에 요일 문자열을 직접 쓰지 말 것.

  Figma 원본은 요일 표기가 세 갈래로 갈려 있었다.
  | Figma 표기 | 실제 | 판정 |
  |---|---|---|
  | 채팅 구분선 `8/12 (수)` · 일정 헤더 `8/13 (목)` | 수 · 목 | ✅ 맞음 |
  | 확정 카드·AI 추천 `8/15 (금)` · STEP 2 `13수` | 토 · 목 | 하루 빠름 |
  | 일정 캘린더에서 1일을 수요일 열에 배치 | 토 | 사흘 빠름 |

  세 갈래 중 실제 달력과 맞는 첫 번째를 채택하고 나머지를 전부 여기서 파생시켰다.
- 일정 추가 STEP 1~3 에는 뒤로가기가 없고, 이미 활성인 일정 탭을 다시 누르면
  `AppNavigator` 의 `resetTo` 가 마법사 스택을 초기화한다.

## 채팅방 홈 색상 (렌더에서 샘플링)

| 용도 | 값 |
|---|---|
| 방 테마 바 | `#FF9900` `#04CDA3` `#75B6FF` `#A100FF` |
| 아바타 틴트 | `#FFE7CA` `#DCF8F2` `#EBF4FF` `#F2DBFF` |
| 활성 방 배경 | `#FFF5EB` |
| 칩 진행중/확정/모집중 | `#FF8C3B` / `#ABABAB` / `#DDECFD`(글자 `#4A90D9`) |

## 참고 — 회원가입 (`309:2256`) 원본 좌표

공통: 뒤로가기 `x16~18 y49~52 14×13`, 타이틀 `x39~41 y45~48 84×22`,
STEP 라벨 `x16~18 y82 83×15`, 진행바 `y77~82 h3~5`,
하단 AccentAndNextButton `x12 y423 197×28`.

### 회원가입/시작 `150:125`
- 로고 main2 `x88 y111 40×40`
- "mealchat 시작하기" `x46 y166 129×10`
- "mealchat에서 한 번에!" `x46 y186 129×10`
- AccentAndNextButton `x12 y238 197×28`
- "이미 계정이 있나요? 로그인" `x63 y276 95×10`

### 회원가입/개인정보 입력 `150:121`
진행바: fill `x18 y77 48×5`, 나머지 3칸 `x68/x112/x156 y78 42×3`
- 닉네임 `x18 y115` → 입력 `x18 y126 184×23` (값 "나야나" `x26 y133`)
- 이메일 `x18 y156` → 입력 `x18 y167 184×23` (값 "nayana@gmail.com")
- 비밀번호 `x18 y198` → 입력 `x18 y209 184×23`
- 비밀번호 재확인 `x18 y240` → 입력 `x18 y251 184×23`
- 계좌번호 `x18 y283` → 은행칩 `x18 y294 51×19` + 번호 `x72 y294 130×19`
- 생년월일 `x18 y322` → 년 `x17 y339 55×23` / 월 `x83 y339 55×23` / 일 `x147 y339 55×23`

### 회원가입/구글 캘린더 연동 `150:122`
진행바: fill `x16 y77 92×5`, 나머지 `x112/x157 y78 42×3`
- 일러스트 박스 `x16 y127 184×190`, moa 이미지 `x56 y158 104×85`
- "mealchat 모아" `x46 y153 129×10`
- 본문 `x23 y274 170×33`
- "필요시 추후 설정에서 변경 가능합니다!" `x34 y321 148×10`

### 회원가입/취향 분석 `150:123`
진행바: fill `x16 y77 136×5`, 나머지 `x154 y78 42×3`
- 일러스트 박스 `x16 y127 184×190`, welling3 `x74 y175 73×88`
- "mealchat 웰링" `x46 y153`, 본문 `x23 y271 170×33`

### 회원가입/약관 동의 `150:126`
진행바: track `x16 y82 180×5`, fill `x16 y82 48×5`
- 일러스트 박스 `x16 y127 184×190`, dudu `x57 y174 107×78`
- 약관 3줄 `x23 y328 169×49`, 체크 아이콘 `x21 y329 / y347 / y366 10×10`
- 구분선 `x21.5 y385 174×0`, "전체 동의" `x16 y385 156×26` + 체크 `x22 y393 10×10`

## 미구현 — 취향게임 (`296:2851`)

6개 화면 동일 구조. 고기 `296:2866` / 해산물 `296:2886` / 양식 `296:2906` /
중식 `296:2926` / 일식 `296:2946` / 한식 `296:2966`

- 타이틀 `x41 y48 84×22`, 뒤로가기 `x18 y52 14×13`, "건너뛰기" `x166 y56 34×8`
- 진행바 `y82 h5` — fill 폭: 29.7 / 59.3 / 89 / 118.7 / 148.3 / 178 (track 은 나머지)
- step 라벨 `x20 y90 107×15`, step 카운터 `x140 y93 60×8`
- 질문 `x20 y118 180×16`
- 음식 이미지 카드 `x20 y148 180×180`
- 좌측 X 버튼 `x22 y378 30×30`, 우측 하트 버튼 `x168 y378 30×30`
- 가운데 캐릭터 썸 `x87 y365.5 46×55`
- "별로예요" `x12 y414 50×8`, "좋아요" `x158 y414 50×8`


## Code Connect

**쓸 수 없다.** Dev 또는 Full 시트 + Organization/Enterprise 플랜이 필요한데 이 파일이
속한 팀은 student 티어이고 시트가 View 다. `get_code_connect_suggestions` 가 그대로
거절한다.

그래서 코드 위치는 Figma 안에 다음 두 곳으로 남긴다. 플랜과 무관하게 동작한다.

- **컴포넌트**: `description` 에 코드 경로를 적는다 (`SettlementCard` 적용 완료)
- **화면 프레임**: `description` 이 없으므로 `annotations` 에 적는다 (`정산` 적용 완료)

플랜이 올라가면 `@figma/code-connect` 를 넣고 `figma.config.json` 을 만든 뒤
`*.figma.ts` 템플릿으로 옮기면 된다. 그때까지는 이 문서의 위 매핑 표가 기준이다.
